# This is a Declarative Automation Bundle definition for axa_dabs.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: axa_dabs
  uuid: 71888562-1858-4a74-957f-dc0c3d9cf18a

include:
  - resources/*.yml

# Variable declarations. These variables are assigned in the dev/prod targets below.
variables:
  catalog:
    description: The catalog to use


targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://dbc-921bc807-9d50.cloud.databricks.com
    variables:
      catalog: dev

  prod:
    mode: production
    workspace:
      host: https://dbc-921bc807-9d50.cloud.databricks.com
      # We explicitly deploy to /Workspace/Users/tlyemul@gmail.com to make sure we only have a single copy.
      root_path: /Workspace/Users/tlyemul@gmail.com/.bundle/${bundle.name}/${bundle.target}
    variables:
      catalog: prod
    permissions:
      - user_name: tlyemul@gmail.com
        level: CAN_MANAGE
